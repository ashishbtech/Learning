#include <stdio.h>
int main()
{
    int num;
    printf("Enter a positive number to get to know whether the number is odd or even\n");
    scanf("%d", &num);
    if (num % 2 == 0)
    {
        printf("the number %d is even\n", num);
    }
    else
    {
        printf("the number %d is odd\n", num);
    }
    return 0;
}
// any number which is divisible by 2 is even
// day 2
/*
#include <stdio.h>
int main()
{
    int num;
    printf("Enter a positive number to get to know whether the number is odd or even\n");
    scanf("%d", &num);
    if (num % 2 == 0)
    {
        printf("the number is even\n");
    }
    if(n %2! = 0)
    {
        printf("the number is odd\n");
    }
    return 0;
}



*/